sudo apt-get install git
mkdir ~/environment
cd ~/environment
git clone https://github.com/nargetdev/ansible.git
cd ansible

## Install Ansible
sudo apt-get install ansible
# now run the thing that opens the ansible GUI / CommandLine Interface
